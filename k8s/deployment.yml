apiVersion: apps/v1
kind: Deployment
metadata:
  name: aquacounter
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aquacounter
  template:
    metadata:
      labels:
        app: aquacounter
    spec:
      serviceAccountName: aquacounter-service-account  # Agrega esta línea
      containers:
      - name: aquacounter
        image: covadongafdez/aquacounter:827a3f9ab273e68f8cdc968a37b864d4b0f24978
        resources:
          requests:
            cpu: 200m
            memory: 300Mi
          limits:
            cpu: 500m
            memory: 500Mi
        envFrom:
        - secretRef:
            name: aquacounter-env
